# Orchestrator env (loaded by code/gateway.py on import)
# auto = free-first failover via gateway LLM_ORDER; oai = pin OpenAI
AGENT_LLM_PROVIDER=auto
LLM_GATEWAY_V10_URL=http://localhost:8110
# Agent Desktop UI (code/run_ui.ps1)
AGENT_UI_PORT=8120
# Legacy (still honoured): LLM_GATEWAY_V9_URL=http://localhost:8109

# Optional: web search (DuckDuckGo works without a key)
TAVILY_API_KEY=
